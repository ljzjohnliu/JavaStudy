<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.study.java">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JavaStudy">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".event.TestEventActivity"
            android:exported="true" />
        <activity
            android:name=".event.TestEventActivity2"
            android:exported="true" />
        <activity
            android:name=".handler.HandlerActivity"
            android:exported="true"
            android:label="Handler简单用法" />
        <activity
            android:name=".handler.HandlerActivity2"
            android:exported="true"
            android:label="HandlerThread和Thread区别" />
        <activity
            android:name=".handler.LeakHandlerActivity"
            android:exported="true"
            android:label="LeakHandlerActivity" />
    </application>

</manifest>